package com.elastisys.scale.commons.net.ssl;

/**
 * Enumeration capturing recognized types of key and trust stores.
 * 
 * 
 * 
 */
public enum KeyStoreType {
	/** Java Key Store (JKS) type of key store. */
	JKS,
	/** PKCS #12 type of key store. */
	PKCS12
}
